// to check the let and const datatype 

//let is the one form of strict type over the var 

// when to make let type variable its perform the hositng but its also knowing timer dead zone because 
// of the let block scope 

// let and const they aren't the global variable (that means its not under the window, this object)
console.log(b);

let b = 78;
var a = 34 ;

console.log(a);